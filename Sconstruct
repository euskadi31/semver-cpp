env = Environment()
env.Decider('MD5')
env.Append(CPPFLAGS = ["-std=c++11", "-pedantic", "-Wall", "-pipe", "-g"])
build_dir = "build"
env.SConscript("SConscript", exports = ["env"])
env.Clean('.', build_dir)
